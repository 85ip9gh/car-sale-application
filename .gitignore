# RSA signing keys and any other certificate material.
*.pem

# Local environment files.
.env
react_frontend/.env.local

# Local MySQL data directory. Never commit database files: they carry password
# hashes and the server's own TLS keys.
.db/

# Deployment secrets.
deploy/app.env
deploy/keys/

# Build output and local tooling.
java_backend_CRUD_1/target/
react_frontend/node_modules/
react_frontend/build/
.idea/
